{% set classes = classes|default('') %}

{% if isCentered|default(false) %}
    {% set classes = classes ~ ' m-auto' %}
{% endif %}

<div class="container {{classes}}">
    {% block content%}{% endblock %}
</div>